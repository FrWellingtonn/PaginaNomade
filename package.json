{
  "name": "nomade",
  "homepage": "https://FrWellingtonn.github.io/PaginaNomade",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {

    "start": "react-scripts start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "react-scripts": "^5.0.1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd nomade-barbearia && npm start",
    "dev": "cd nomade-barbearia && npm start",
    "build": "cd nomade-barbearia && npm run build"

  }
}
